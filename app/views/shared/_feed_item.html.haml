%li{:id => "#{feed_item.id}"}
  / = link_to gravatar_for(feed_item.user), feed_item.user
  %span.user
  %span.content= feed_item.content
  %span.timestamp
    Posted #{time_ago_in_words(feed_item.created_at)} ago.

  = link_to "削除", project_task_path(task_id: feed_item.id, project_id: @project.id), method: :delete, data: { confirm: "削除しますか？" }, title: feed_item.content
  = link_to "開始", task_start_path(task_id: feed_item.id, project_id: @project.id), method: :start
  = link_to "完了", task_finish_path(task_id: feed_item.id, project_id: @project.id), method: :finish