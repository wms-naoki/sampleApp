%h1 Listing projects

%table
  %thead
    %tr
      %th プロジェクト一覧
      %th
      %th
      %th

  %tbody
    - @projects.each do |project|
      %tr
        %td= project.name
        %td= link_to '開く', project
        %td= link_to '編集', edit_project_path(project)
        %td= link_to '削除', project, method: :delete, data: { confirm: '削除しますか' }

%br

= link_to '新規プロジェクトの作成', new_project_path
